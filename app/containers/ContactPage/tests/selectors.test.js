// import { fromJS } from 'immutable';
// import { makeSelectContactPageDomain } from '../selectors';

// const selector = makeSelectContactPageDomain();

describe('makeSelectContactPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
